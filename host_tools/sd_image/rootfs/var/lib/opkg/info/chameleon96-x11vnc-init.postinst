#!/bin/sh
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl $OPTS enable chameleon96-x11vnc-init.service

	if [ -z "$D" -a "enable" = "enable" ]; then
		systemctl --no-block restart chameleon96-x11vnc-init.service
	fi
fi
