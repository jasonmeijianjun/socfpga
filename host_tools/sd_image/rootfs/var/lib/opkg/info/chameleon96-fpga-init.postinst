#!/bin/sh
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

if type systemctl >/dev/null 2>/dev/null; then
	systemctl $OPTS enable chameleon96-fpga-init.service

	if [ -z "$D" -a "enable" = "enable" ]; then
		systemctl --no-block restart chameleon96-fpga-init.service
	fi
fi
