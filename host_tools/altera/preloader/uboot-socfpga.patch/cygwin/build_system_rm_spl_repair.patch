diff -rupN uboot-socfpga.orig/Makefile uboot-socfpga/Makefile
--- uboot-socfpga.orig/Makefile	2013-07-26 12:50:23.344553400 -0700
+++ uboot-socfpga/Makefile	2013-07-26 12:50:54.850703700 -0700
@@ -877,7 +877,9 @@ clobber:	tidy
 	@rm -f $(obj)spl/{u-boot-spl,u-boot-spl.bin,u-boot-spl.map}
 	@rm -f $(obj)spl/{u-boot-spl.lds,u-boot.lst}
 	@rm -f $(obj)MLO MLO.byteswap
+ifneq ($(HOSTOS),cygwin)
 	@rm -f $(obj)SPL
+endif
 	@rm -f $(obj)tools/xway-swap-bytes
 	@rm -f $(obj)arch/powerpc/cpu/mpc824x/bedbug_603e.c
 	@rm -f $(obj)arch/powerpc/cpu/mpc83xx/ddr-gen?.c
